package com.siso.retrofitexample;

public interface IStartActivty {

     void login(String email , String password);

     void register(String email , String password);

    void inflateFragment(String fragmentTag);
}
